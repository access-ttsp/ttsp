Task:
- Ensure that when a user registers in better-auth, they are also created in the app database
- Create a `users` table in the app database where a copy of the user from better-auth will be stored with the same id
- This is simply a table to enable enforcing constraints on users in the app database

Important:
- **Do not do anything outside the current task.**
- Before implementing, analyze my solution and state the pros and cons.

Preparation:
- Study Drizzle postgres documentation
- Study better-auth documentation

